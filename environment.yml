name: spatios2e
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy>=1.23
  - scipy>=1.10
  - pandas>=1.5
  - pyyaml>=6.0
  - h5py>=3.8
  - pytorch>=2.0
  - torchvision>=0.15
  - pillow>=9.0
  - tqdm>=4.64
  - pytest>=7.0
  - pip:
      - -e .[preprocess,dev]
